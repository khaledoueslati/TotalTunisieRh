{% extends '::base.html.twig' %}

{% block body -%}
    <h1>DirectionsPostes</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Iddirectionpostes</th>
                <td>{{ entity.idDirectionPostes }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('directionspostes') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('directionspostes_edit', { 'id': entity.idDirectionPostes }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
