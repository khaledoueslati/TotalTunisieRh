{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Directions</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Iddirection</th>
                <td>{{ entity.idDirection }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entity.libelle }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('directions') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('directions_edit', { 'id': entity.idDirection }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
